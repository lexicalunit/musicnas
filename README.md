# usage

```bash
# if you haven't done it before, download a local mirror of music:
./download.sh

# after that, put new music in the `new` directory tree, to be synced up to NAS with:
./upload.sh
```

